name: f5_vpn
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  openvpn_flutter: ^1.3.4 # 👈 Terminal ညွှန်းတဲ့အတိုင်း ဗားရှင်းအမှန် ပြောင်းလိုက်ပါပြီ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true